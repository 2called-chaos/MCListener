<?php

$aliases = 'players, users, userlist, playerlist, online';

function CMD_list($MCL, $user, $params = array())
{
  $MCL->pm($user, 'Sorry not implemented!');
  //   $players = $this->_cmd_getPlayers();
  //   // $this->mcexec('');
  // break;
}